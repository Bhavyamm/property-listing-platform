import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['localhost', "https://property-listing-platform-p9k2-f7cd97glu-bhavyamms-projects.vercel.app"]
  },
};

export default nextConfig;
